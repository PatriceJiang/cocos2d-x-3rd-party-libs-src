diff --git a/Configurations/15-android.conf b/Configurations/15-android.conf
index d3b428b..8ec3907 100644
--- a/Configurations/15-android.conf
+++ b/Configurations/15-android.conf
@@ -14,6 +14,10 @@
         mips64 => "mips64el-linux-android",
         x86    => "i686-linux-android",
         x86_64 => "x86_64-linux-android",
+        "android-arm"    => "arm-linux-androideabi",
+        "android64-aarch64"  => "aarch64-linux-android",
+        "android-x86"    => "i686-linux-android",
+        "android-x86_64" => "x86_64-linux-android",
     );
 
     sub android_ndk {
@@ -23,6 +27,8 @@
             }
 
             my $ndk = $ENV{ANDROID_NDK};
+            print "#############NDK######################\n";
+            print "$ndk"."\n";
             die "\$ANDROID_NDK is not defined"  if (!$ndk);
             die "\$ANDROID_NDK=$ndk is invalid" if (!-d "$ndk/platforms");
             $ndk = canonpath($ndk);
@@ -64,8 +70,17 @@
             }
             die "no sysroot=$sysroot"   if (!-d $sysroot);
 
+            print "#########SSYROOT-1##########################\n";
+            print "$sysroot"."\n";
+
             $sysroot =~ m|/android-([0-9]+)/arch-(\w+)/?$|;
             my ($api, $arch) = ($1, $2);
+            my $api = 21;
+            #my $arch = "arm";
+            print "#########SSYROOT-2##########################\n";
+            print "$sysroot"."\n";
+            print "#########API/PATH##########################\n";
+            print "$api"."/"."$arch"."\n";
 
             my $triarch = $triplet{$arch};
             my $cflags;
@@ -86,10 +101,14 @@
             } elsif ($user{CC} eq "clang") {
                 die "no NDK clang on \$PATH";
             } else {
+                print "----- android gcc : $triarch-gcc, arch $arch\n";
+                print "----- which " . which("$triarch-gcc");
+
                 if (which("$triarch-gcc") !~ m|^$ndk/.*/prebuilt/([^/]+)/|) {
-                    die "no NDK $triarch-gcc on \$PATH";
+                    #die "no NDK $triarch-gcc on \$PATH";
                 }
-                $cflags .= " -mandroid";
+                $cflags .= " -mandroid".
+                    " -D__ANDROID_API__=$api";
                 $user{CROSS_COMPILE} = "$triarch-";
             }
 
@@ -103,9 +122,14 @@
                 $cppflags .= " -isystem \$(ANDROID_NDK)/$incroot";
             }
 
+            print "=========================SYSROOT===============\n";
+            print "$ndk \n";
+            print "=========================SYSROOT===============\n";
+            print "$sysroot \n";
+
             $sysroot =~ s|^$ndk/||;
             $android_ndk = {
-                cflags   => "$cflags --sysroot=\$(ANDROID_NDK)/$sysroot",
+                cflags   => "$cflags --sysroot=$sysroot",
                 cppflags => $cppflags,
                 bn_ops   => $arch =~ m/64$/ ? "SIXTY_FOUR_BIT_LONG"
                                             : "BN_LLONG",
